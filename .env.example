# Origin of the clic3d.tn parent page that embeds CADAM via iframe.
# Used to validate inbound postMessage events (SSO handshake).
VITE_CLIC3D_PARENT_ORIGIN=https://clic3d.tn

# Optional additional Google AI Studio keys (each = separate Google Cloud project).
# Round-robin'd with GOOGLE_API_KEY for higher throughput.
GOOGLE_API_KEY_2=
GOOGLE_API_KEY_3=
GOOGLE_API_KEY_4=
